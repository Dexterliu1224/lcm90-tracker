# lcm90-tracker · 项目接续总结

> **这份文件的用途**：任何时候（换电脑、隔了一周、换一个新的 AI 会话）打开它，
> 都能在 5 分钟内重建全部上下文，直接接着往下写代码。
> **最后更新**：2026-08-04（真机验证状态已按用户反馈修正） ｜ **对应提交**：`a6c8cb8`（2026-08-02）

---

## 一句话

框选画面里的任意目标，让星特朗 LCM90 望远镜自动跟着它转 —— 不需要轨道数据、
不需要知道目标是什么，AI 视觉跟的就是你框出来的那个东西。

---

## 5W1H

| | |
|---|---|
| **What（做什么）** | 一套「视觉伺服 + 望远镜控制」的桌面软件。核心链路：相机取帧 → 框选目标 → 视觉跟踪器算出目标偏离画面中心多少像素 → 标定矩阵把像素误差换算成两轴角速度 → PID 输出速率 → NexStar 串口下发给 LCM90 基座 → 目标被持续摁在画面中心。 |
| **Why（为什么）** | LCM90 自身只会按恒星速率慢慢转，跟不了飞机、无人机、卫星这类动目标。市面方案要么依赖 TLE 轨道根数（没根数的目标就没辙），要么要接专业赤道仪。这个项目走「纯视觉」路线：**看得见就跟得上**，且面向中学生也能操作的易用性。 |
| **Who（给谁用）** | 主要是教学/科普场景 —— 中学生、教师。所以有出厂账号体系、中文全流程提示、《使用说明书》分步手册、录制回放功能。同时也是作者自己接真机做观测的工具。 |
| **Where（跑在哪）** | Windows 为主（有 `START-Windows.bat` / `UPDATE-Windows.bat` / `DIAGNOSE-Windows.bat` 和 GitHub Actions 打包 exe），macOS 可开发调试。界面是 FastAPI + 网页，但装在**原生桌面窗口**里（Windows 走 Edge WebView2，macOS 走 WebKit），不是浏览器。 |
| **When（时间线）** | 2026-07-31 首次提交，2026-08-02 最后一次提交。**3 天 21 个提交**，密度极高。 |
| **How（怎么实现）** | Python 3.9 兼容。FastAPI 后端 + 单页网页前端 + pywebview 窗口外壳。视觉用经典算法不用神经网络（CSRT 跟踪器，失败自动降级到模板匹配；星点检测用「背景估计→阈值分割→连通域→亮度加权质心」）。控制用双线程状态机 + 绝对速度前馈 PID。 |

---

## 技术架构（`CONTRACT.md` 定义了模块间接口，签名不得改动）

```
serve.py            启动入口（--browser 退回浏览器 / --no-window 无头 / --verbose 排查 / --check 自检）
config.yaml         串口、相机、PID、标定步长、账号、录制 全部配置
core/
  mount.py          NexStar 协议 + SimMount 仿真基座（速率上限/加速度/齿隙都仿真了）
  camera.py         OpenCVCamera(UVC) + SimCamera（合成星空，画面随指向变化 → 真闭环）
  qhy.py            QHY 相机官方 SDK 通道
  tracker.py        框选目标跟踪：CSRT → 模板匹配自动降级、置信度、丢失重捕
  stars.py          星点自动识别 + 标定星推荐 + FWHM 锐度（对焦辅助）
  calibration.py    四步自动标定（移动中喂帧、目标漂移补偿、质量闸）
  control.py        状态机 + 双线程闭环 + 绝对速度前馈 PID
  auth.py           PBKDF2-HMAC-SHA256 加盐散列账号库 + 内存会话
  recorder.py       定频写视频（mp4 → avi 自动降级、时长上限保护）
app/                FastAPI 后端 + 深空风格网页（含登录页）
tests/              test_core.py / test_auth_api.py / test_frontend_contract.py
```

**规模**：20 个 Python 文件，约 **7,537 行**。依赖：fastapi / uvicorn / pyyaml / numpy /
opencv-python / pyserial / pillow / pywebview。

---

## 进展：21 个提交在做什么（倒序）

| 阶段 | 提交 | 干了什么 |
|---|---|---|
| **① 骨架**（7-31） | 1 个 | 手动框选 + AI 视觉自动跟踪，端到端跑通 |
| **② 打通硬件**（7-31） | 8 个 | 接 QHY 相机官方 SDK、SDK 路径自动搜索、`/api/qhy/diagnose` 诊断接口、串口选择覆盖 config、修一批 Windows 兼容与批处理语法坑 |
| **③ 标定可靠性**（7-31） | 3 个 | 标定步长自适应（按实测像元尺度定步长，丢目标自动减半重试）、反向标定拦截、锁定即停转 |
| **④ 产品化**（8-01） | 5 个 | 登录/账号管理 + 录制、改成独立桌面程序（原生窗口、onedir 打包、无控制台）、星点自动识别与标定星推荐 |
| **⑤ 稳健性**（8-01） | 3 个 | 「点退出没反应」（轮询打断跳转 + 点击区域过小）、多智能体审查后修复 6 处缺陷（含「关窗后望远镜还在转」）、窗口打不开改用 Edge 应用模式不再依赖 .NET |
| **⑥ 交付**（8-02） | 1 个 | 《使用说明书》面向中学生的分步操作手册 |

**这条曲线很清楚**：跑通 → 接硬件 → 修可靠性 → 做成产品 → 补文档。

---

## 成果与实测效果（README「已验证」段的原始数据）

**仿真端到端已验证：**

| 指标 | 实测值 |
|---|---|
| 标定反解误差 | **<1%**（5.98 vs 6.00 角秒/像素，含转角与镜像判定） |
| 连续跟踪 | **36 秒全程锁定**，中位残差 **5 px**，最大 22 px |
| 遮挡重捕 | 目标被遮挡 **10 帧**能重捕 |
| 断流保护 | 相机断流 **0.5 秒内**基座自动停转 |
| 星点检测 | 720p 一帧约 **30 ms**，质心精度 **0.03 像素** |

**做出来的可交付形态：**

- 独立桌面程序（不是"跑个脚本开浏览器"），关窗即退出，退出会自动停跟踪、停基座、收口录像
- 端口被占自动顺延，WebView2 缺失自动退回浏览器并说明原因
- 账号体系：出厂 admin/admin + 强制改密提醒 + PBKDF2 散列 + admin 可管账号
- 录制带标注画面（跟踪框、准星环、误差读数），教学回放学生能看懂
- 星点识别三色圈：蓝=可选、橙虚线=过曝/过大不适合、**绿=推荐标定星**（按「不过曝/够亮/无邻星/离边缘远」四条挑）
- FWHM 锐度读数做对焦辅助 —— 转调焦座让数字变小，最小即最准

**硬编码的安全设计（不是配置项，不能被改坏）：**

1. 目标丢失 → 基座速率**立即清零**（绝不带旧速率盲转）
2. 相机断流 0.5 秒 → 停转并提示
3. 未标定 → **拒绝**开始跟踪（方向未知时闭环会把目标越推越远）
4. 停轴指令两轴独立下发（一轴失败不影响另一轴停下）

---

## 当前状态

| 项 | 状态 |
|---|---|
| 分支 | `main` |
| 工作区 | **干净**，无未提交改动 |
| 与远程 | **已同步**（`origin/main` = 本地 HEAD = `a6c8cb8`） |
| 远程仓库 | https://github.com/Dexterliu1224/lcm90-tracker |
| CI | `.github/workflows/build-windows.yml`（打 Windows exe + 冒烟测试） |
| 测试 | 3 个测试文件；跑测试需额外装 `httpx` |
| **真机** | ✅ **串口通 / 相机接 / 标定做过 / 跟到真实目标成功**（2026-08-04 确认）；仅缺实测残差数据 |

---

## ✅ 真机状态：全链路已在真实硬件上跑通

> **2026-08-04 用户确认**。这是三个项目里**完成度最高、也是唯一走完
> "仿真 → 真机 → 跟到真实目标"完整闭环**的一个。

| 环节 | 真机状态 |
|---|---|
| NexStar 串口通信 | ✅ **已通** —— 协议在真实手控器上验证通过 |
| 相机接入 | ✅ **已接** |
| 光学标定 | ✅ **已做** |
| 跟踪真实目标 | ✅ **成功** |
| 实测残差 | ❓ **未测量** ← 唯一的数据缺口 |

**→ 这意味着 README 里「未上过真机」那一整段描述已经彻底过期，
应该改写成真机验证记录。** 这是本项目现在最值钱的资产：
三个项目里只有它有真机数据，而这些经验是 `telescope-avx8` 直接能复用的。

### 唯一还缺的一件事：把残差量出来

跟踪已经成功了，但**没有量化数据**。仿真里的基准是「中位残差 5px、最大 22px」，
真机到底是多少不知道。补上这个数字，项目就从"能用"变成"可验证、可对外说"。

怎么补（界面上已经有读数，只是没记录下来）：

- 跟踪时界面实时显示残差和误差读数 → 跟一段稳定目标，**记下中位值和最大值**
- 更省事的办法：**点「录制」录一段**。录的就是带残差读数的标注画面，
  事后回放逐帧读，还能直接当演示素材（`data/recordings/`）
- 顺手也记一下：丢失重捕成功没有、跟了多久没掉

### 其他值得补记的（有就写，没有就算）

- [ ] 实际用的串口线型号 / 串口号
- [ ] 标定解出来的 `camera_angle_deg` / `flip_x` / `flip_y` 实测值
- [ ] 真机整定后的 PID 参数，与仿真默认值差多少
- [ ] 接的是 QHY 还是普通 UVC 相机
- [ ] 跟的是什么目标（飞机？无人机？亮星？）

**接真机的标准顺序**（留作复查用）：

1. 手控器开机（这套流程**不需要** SkyAlign 校准，可跳过）
2. NexStar 串口线插**手控器底部的 RJ-22 小方孔**（⚠️ 不是基座的 AUX 口，那是另一套协议）
3. 界面选串口 → 连接
4. **先用手动微调按钮确认四个方向都动、且方向正确**
5. 再做标定 —— 框一个**静止参照物**（远处建筑角、亮星），点「自动标定」，望远镜自己走四步
6. 最后才框目标开始跟踪

---

## 下一步可以接着做的（按优先级）

> ~~上真机验证串口~~ / ~~相机~~ / ~~标定~~ / ~~跟真实目标~~ —— **全部✅已完成**（2026-08-04）。
> 项目已经从"能不能用"阶段毕业了，剩下的是"用得多好、别人信不信"。

1. **量出真机残差** —— 唯一的数据缺口。跟一段稳定目标，记中位/最大残差，
   或直接点「录制」录一段带读数的画面事后读。与仿真基准 5px / 22px 对照。
   **没有这个数字，前面所有成果都只能说"跑通了"，说不出"跟得多准"。**
2. **改 README** —— 「未上过真机」那段已经完全过期，换成真机验证记录 + 实测残差。
   这是别人打开仓库最先看到的东西，现在写的是三天前的状态。
3. **把真机踩的坑和实测参数落到代码里** —— 时序差异写进 `core/mount.py` 注释，
   标定值和整定后的 PID 写回 `config.yaml`。别只留在脑子里，换台电脑就没了。
4. **把真机经验反哺给 `telescope-avx8`** —— 那边用的是**同一套 NexStar 协议**，
   而且是三个项目里唯一还没上过真机的。你这边趟过的坑直接能省它一整轮调试。
   建议在动 avx8 之前，先把这边的经验写成几条清单。
5. 补测试：`tests/` 目前 3 个文件，`stars.py` / `recorder.py` 的覆盖可以再厚一点。

---

## 快速恢复上下文的命令

```bash
cd ~/lcm90-tracker
git log --oneline -15                    # 最近在改什么
git status                               # 有没有没提交的活
cat CONTRACT.md                          # 模块接口契约（改代码前必读）
python3 -m venv ~/.telescope-venv && ~/.telescope-venv/bin/pip install -r requirements.txt
~/.telescope-venv/bin/python serve.py    # 不用任何硬件就能跑起来看效果
# 仿真基座 + 仿真画面 → 框亮星 → 自动标定 → 框小飞机 → 开始跟踪
```

**改代码前先读 `CONTRACT.md`**：它规定了各模块的接口签名不得改动，
且要求 Python 3.9 兼容（文件首行加 `from __future__ import annotations`，
不用 `X | Y` 类型语法、不用 `match`），全部注释与用户可见文案用中文。
