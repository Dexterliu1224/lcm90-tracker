# LCM90 视觉跟踪台 · 使用说明书

> 给第一次用这套设备的老师和同学。照着章节顺序做，不用跳着看。
> 遇到问题直接翻到最后一章「出问题了怎么办」。

---

## 目录

1. [这套软件是干什么的](#1-这套软件是干什么的)
2. [先准备这些东西](#2-先准备这些东西)
3. [第一步：把软件装到电脑上](#3-第一步把软件装到电脑上)
4. [第二步：接线](#4-第二步接线)
5. [第三步：第一次启动](#5-第三步第一次启动)
6. [第四步：连接望远镜](#6-第四步连接望远镜)
7. [第五步：打开画面](#7-第五步打开画面)
8. [第六步：对焦](#8-第六步对焦)
9. [第七步：标定（最重要的一步）](#9-第七步标定最重要的一步)
10. [第八步：开始跟踪](#10-第八步开始跟踪)
11. [录像](#11-录像)
12. [账号管理](#12-账号管理)
13. [参数设置（config.yaml）](#13-参数设置configyaml)
14. [出问题了怎么办](#14-出问题了怎么办)
15. [一页速查卡](#15-一页速查卡)

---

## 1. 这套软件是干什么的

你在画面里**框住一个东西**，望远镜就会自动转动，一直把它保持在画面中央。

不需要知道那个东西是什么，也不需要输入任何轨道数据——软件靠的是**看**：
它盯着你框的那一小块画面，发现目标偏了，就指挥望远镜往回转。

能跟什么：亮星、行星、飞机、卫星光点、无人机——只要在画面里看得见、且移动不是快得离谱。

**软件不能做什么**（先说清楚，免得白折腾）：

- 不能帮你找到目标。目标得先出现在画面里，你才能框它。
- 不能跟太阳。**绝对不要用望远镜对着太阳**，会烧坏眼睛和相机。
- 不能在完全没有光的画面里工作。看不见就跟不了。

---

## 2. 先准备这些东西

### 必需的

| 东西 | 说明 |
|---|---|
| 星特朗 LCM90 望远镜 | 带手控器（就是那个有按键和屏幕的小盒子） |
| Windows 电脑 | Win10 或 Win11 都行 |
| NexStar 串口线 | 一头是小方口（RJ-22），插手控器；另一头接电脑 |
| USB 转串口线 | 如果电脑没有九针串口（现在的电脑基本都没有），需要这个 |
| 相机 | 见下面「相机怎么选」 |

### 相机怎么选

**两种接法，各有各的用处，可以都装：**

**① 目镜相机**——相机装在望远镜的调焦座上（就是平时插目镜的地方）。

- 好处：放大倍数高，看得清，跟得准
- 坏处：视野很窄（大约 1.2°×0.7°，比小拇指指甲盖伸直手臂后挡住的天空还小），找目标很费劲

**② 外部相机**——相机和望远镜并排绑在一起，朝同一个方向。

- 好处：视野大，容易先找到目标
- 坏处：放大倍数低，精度不如目镜相机

**教学建议**：先用外部相机把目标找进画面，跟稳了再换目镜相机看细节。

**相机型号**：任何能被 Windows 识别成"摄像头"的 USB 相机都行。QHY 天文相机也支持，但需要先装官方驱动（见下）。

### 可选的

- **QHY 相机驱动**：用 QHY 相机才需要。到 <https://www.qhyccd.com/download/> 下载「AllInOne」驱动包装上。
- **USB 转串口驱动**：串口线插上电脑后如果设备管理器里认不出来，需要装。常见的芯片是 CH340 和 FTDI，搜芯片名字就能找到驱动。

---

## 3. 第一步：把软件装到电脑上

**推荐用打包版**，不用装 Python，下载解压就能跑。

### 3.1 下载

1. 用浏览器打开 <https://github.com/Dexterliu1224/lcm90-tracker/actions>
2. **先登录 GitHub 账号**（不登录看不到下载区）
3. 点最上面那次带绿色对勾 ✅ 的运行
4. 页面拉到**最底部**，找到 **Artifacts** 区域
5. 点 `lcm90-tracker-windows` 开始下载

### 3.2 解压和放置

把下载的压缩包解压出来，你会看到：

```
lcm90-tracker.exe      ← 主程序，双击这个
_internal\             ← 程序零件，不能删、不能改名、不能单独搬走
config.yaml            ← 配置文件，必须和 exe 待在一起
README.md              ← 技术说明
data\                  ← 软件自己用的数据（标定、账号、录像都在这）
```

**三条铁规矩：**

1. **`_internal` 文件夹不能删。** 删了程序打不开。
2. **`config.yaml` 必须和 exe 在同一个文件夹。** 找不到它程序会直接退出。
3. **要移动软件，整个文件夹一起搬。** 别只把 exe 拷走。

**放哪儿**：建议放在一个**路径不带中文**的地方，比如 `D:\telescope`。放在中文路径下多数时候也能用，但出问题时会难查。

### 3.3 （备选）源码版

如果你要改代码或者拿不到打包版：

1. 装 Python 3（<https://www.python.org/downloads/>）。**安装第一屏底部的「Add Python to PATH」这个勾必须打上**，不打后面全都会失败。
2. 双击 `START-Windows.bat`，第一次会自动下载依赖包，需要 3~10 分钟，只做一次。
3. 网络慢装不上的话，在文件夹里打开命令行跑：
   ```bash
   .venv\Scripts\python.exe -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
   ```

---

## 4. 第二步：接线

**先接线，再开电脑软件。** 顺序反了也不会坏，只是要重新扫描设备。

### 4.1 望远镜

1. 望远镜装好、上好电池（或接好电源）
2. **手控器开机**
3. 串口线的小方口插进**手控器底部**的口

   ⚠️ **注意**：是插**手控器底部**，不是望远镜底座上的 AUX 口。这是最容易搞错的一步。

4. 另一头接电脑（没有串口就通过 USB 转串口线接）

**关于 SkyAlign 对准**：手控器开机后会问你要不要做对准。**这套跟踪软件不需要对准，可以跳过。** 对准只影响手控器自己的 GoTo 功能。

### 4.2 相机

- **目镜相机**：拧下目镜，把相机装到调焦座上，USB 线接电脑
- **外部相机**：和镜筒并排固定好，**尽量让两者朝同一个方向**，USB 线接电脑

### 4.3 检查接线

打开 Windows 的**设备管理器**（右键开始菜单就有）：

- 「端口 (COM 和 LPT)」下面应该有一个 COM 口，记住编号（比如 COM3）
- 「照相机」或「图像设备」下面应该能看到你的相机

看不到的话，说明驱动没装或者线没插好，先解决这个再往下走。

---

## 5. 第三步：第一次启动

### 5.1 启动

双击 `lcm90-tracker.exe`。

**你会看到**：几秒后弹出一个窗口，深蓝色星空背景，中间是登录框。

**如果没弹出窗口**，看第 14 章的「窗口打不开」。

### 5.2 登录

出厂账号：

| 用户名 | 密码 |
|---|---|
| `admin` | `admin` |

输进去，点「进入控制台」。

### 5.3 立刻改密码

进去以后，页面顶部会有一条**黄色警告条**："还在用出厂密码，任何人都能打开这台望远镜。"

点它右边的「立即修改」，或者右上角的「修改密码」：

1. 原密码填 `admin`
2. 新密码填你自己的（**至少 4 位**）
3. 再输一遍
4. 点「确认修改」

黄条消失就说明改好了。

> **忘记密码怎么办**：关掉软件，把程序目录下的 `data\users.json` 删掉，重新启动，账号会变回 `admin` / `admin`。
> 注意：只删这一个文件。同目录下的 `calibration.json`（标定结果）和 `recordings`（录像）删了就找不回来了。

---

## 6. 第四步：连接望远镜

界面右边第一个框就是「望远镜」。

1. 下拉框里选你的串口（比如 `COM3`）
   - 不知道选哪个？看设备管理器里的编号
   - 只想先试试软件、手上没有望远镜？选 **「仿真基座（无硬件演示）」**
2. 点「连接」

**你会看到**：顶部的「基座 未连接」变成绿色的「基座 已连接」，下方显示固件版本。

**连不上**：会弹出一个排查清单，照着一条条查。最常见的三个原因是——手控器没开机、线插错了口（应该插手控器底部）、串口号选错。

---

## 7. 第五步：打开画面

界面右边第二个框是「视频源」。

### 7.1 选择相机类型

上面两个大按钮，选一个：

- **目镜画面**——相机装在调焦座上
- **外部相机**——并联的广角相机

**这个选择会影响标定的计算，要选对。**

### 7.2 打开

1. 下拉框里选你的相机
   - 没有硬件想先试试？选 **「仿真画面（无硬件演示）」**，会出现一片星空和一架飞来飞去的小飞机
2. 点「开启」

**你会看到**：左边取景器里出现实时画面，中间有一个十字准星和一个圆环。

---

## 8. 第六步：对焦

**画面糊的话，后面全都白搭。** 标定会不准，跟踪会丢目标。

软件提供了一个很好用的对焦工具：

1. 点下方的「**识别星点**」按钮
2. 画面里的星点会被圈出来，下方出现一行字：
   `找到 XX 个星点 · 星点锐度 X.XX 像素`
3. **慢慢转动望远镜的调焦手轮**，盯着那个「星点锐度」数字
4. **数字越小越清晰。** 转到最小的那个位置，就是焦点

> 「星点锐度」的学名叫 FWHM（半高全宽），意思是一个星点在画面上摊开了多少像素。
> 焦点对准时星点最小最锐，这个数字就最小。这是天文观测里最标准的对焦方法。

---

## 9. 第七步：标定（最重要的一步）

### 9.1 标定是干什么的

软件看到"目标偏左了"，但它**不知道该往哪个方向转望远镜**才能把目标move回来——

因为相机可能是歪着装的、可能是倒着装的，望远镜转一度在画面上对应多少像素也不知道。

**标定就是让软件自己搞清楚这件事**：它会让望远镜朝四个方向各走一小步，看目标在画面里往哪儿挪了多少，然后算出「画面偏移」和「望远镜转动」之间的换算关系。

**标定只需要做一次。** 做完存在硬盘上，关机重启还在。

**什么时候必须重新标定：**

- 拆装过相机、转动过相机的角度
- 换了另一个相机
- 从「目镜画面」切换到「外部相机」（或者反过来）

### 9.2 选一个合适的标定目标

**这一步做错，后果非常严重**：解出来的方向可能是反的，跟踪一开就把目标越推越远，而界面上看起来一切正常。

**必须用静止的东西：**

- ✅ 亮星（最好）
- ✅ 远处建筑物的尖角、避雷针
- ❌ **绝对不能用**：飞机、云、鸟、汽车——任何自己会动的东西

**让软件帮你选**：点「识别星点」，画面上会出现三种圈：

| 圈的样子 | 意思 |
|---|---|
| 🔵 蓝色圆圈 | 正常星点，可以点选 |
| 🟠 橙色虚线圈 | 太亮（过曝）或太大（月亮、行星、飞机），不适合 |
| 🟢 绿色圆圈 + 「建议用它标定」 | **软件挑出来的最佳标定星，直接点它** |

绿圈是按四个条件挑的：不过曝、够亮、周围没有别的星干扰、离画面边缘够远。

### 9.3 开始标定

1. 点绿圈选中那颗星（或者自己在画面上**拖一个框**框住目标）
2. 点「**自动标定**」
3. 望远镜会自己动四下，大约半分钟
4. 画面上有进度条，别碰设备，等它走完

**成功**：提示「标定完成：X.XX 角秒/像素，相机转角 X.X°」，顶部的「未标定」变成绿色的「已标定」。

**失败**：软件会明确告诉你原因，最常见的两条——

- 「标定结果不自洽……最常见的原因是标定时框了自己在动的目标」→ 换一颗静止的星重来
- 「目标离画面边缘太近」→ 先用手动微调把目标挪到画面中间

> **软件会拒绝保存不可靠的标定结果**，这是有意设计的。宁可让你重做一次，也不能存一份方向错的数据。

---

## 10. 第八步：开始跟踪

1. 在画面里**拖一个框**框住你要跟的目标
   - 框稍微比目标大一点，别死贴着
   - 框对比度高、边界清楚的东西，效果最好
2. 点「**开始跟踪**」

**你会看到**：

- 顶部状态变成「跟踪中」
- 目标周围有一个框：**黄色**=正在往中间赶，**绿色**=已经跟上了
- 目标进入中间那个圆环（锁定环）后，**望远镜就停下来了**——这就叫跟上了
- 遥测面板里「目标偏差」的数字会变小

### 遥测面板怎么看

| 名称 | 意思 |
|---|---|
| 方位 / 仰角 | 望远镜现在指向哪里（度） |
| 方位速率 / 仰角速率 | 望远镜正在以多快的速度转（度/秒） |
| 目标偏差 | 目标离画面中心差多少像素。**越小越好** |
| 跟踪置信度 | 软件对"我认得这个目标"的把握。低于 30% 会判定丢失 |
| 画面 / 回路 | 相机每秒多少帧、控制回路每秒算多少次 |

### 目标丢了怎么办

目标飞出画面或者被云挡住，软件会**立刻停止望远镜**并显示「目标丢失」。这是安全设计——不停的话望远镜会一直转到限位。

目标回来后软件会自动重新跟上。如果一直不回来，点「重选目标」重新框。

### 手动微调

目标不在画面里？用右下角的方向盘（▲▼◀▶），每点一次转 0.1°。

**注意**：跟踪中和标定中不能用手动微调，会打架。

---

## 11. 录像

点「**录制**」开始，再点一次（此时显示「停止录制」）结束。

- **录的是带标注的画面**——跟踪框、准星环、状态文字都录进去，回放时能看出软件在做什么，适合上课演示
- **存在哪**：程序目录下的 `data\recordings\`
- **文件名**：`日期-时间-用户名.mp4`，比如 `20260802-143025-admin.mp4`
- **录制中**：顶部有红色闪烁标记和计时，下方显示文件名、时长、大小
- **最长 60 分钟**自动停，防止忘了关把硬盘写满（可以在配置里改）

---

## 12. 账号管理

只有管理员（admin）能看到「账号管理」按钮。

**新建账号**：填用户名、密码，选「普通」或「管理员」，点添加。

- 用户名只能用**字母、数字、下划线、减号**，2~24 位（不能用中文）
- 密码至少 4 位

**两种角色的区别：**

| | 普通账号 | 管理员 |
|---|---|---|
| 操作望远镜 | ✅ | ✅ |
| 改自己的密码 | ✅ | ✅ |
| 新建/删除账号、给别人重置密码 | ❌ | ✅ |

**注意**：不能删除自己正在用的账号，也不能删掉最后一个管理员（删了就没人能管账号了）。

**退出登录**：右上角「退出登录」。注意这只是回到登录页，**不是关闭软件**——关软件请直接关窗口。

---

## 13. 参数设置（config.yaml）

用**记事本**打开程序目录下的 `config.yaml` 就能改。**改完必须重启软件才生效。**

> 建议先把原文件复制一份备份再改。

### 望远镜（mount）

| 参数 | 默认 | 什么时候改 |
|---|---|---|
| `max_rate_deg_s` | 2.5 | 望远镜最大转速（度/秒）。LCM90 机械上限约 3，留了余量。**别调超过 3** |
| `min_rate_arcsec_s` | 2.0 | 小于这个速度就不下发指令，防止齿轮在间隙里空蹭 |
| `alt_limit_min_deg` | 5.0 | 仰角下限。**室内把镜筒平放测试时改成 -10**，否则一标定就被限位挡住 |
| `alt_limit_max_deg` | 88.0 | 仰角上限。别改到 90，接近天顶时方位轴要转得飞快 |

### 相机（camera）

| 参数 | 默认 | 说明 |
|---|---|---|
| `width` / `height` | 1280 / 720 | 画面分辨率。调低能提高帧率 |
| `fps` | 30 | 帧率。这是"请求值"，相机不一定给得了 |
| `qhy.exposure_ms` | 20 | QHY 曝光时间（毫秒）。**画面太暗调大，拖影明显调小** |
| `qhy.gain` | 180 | QHY 增益。太暗调大，噪点多调小 |
| `qhy.usb_traffic` | 30 | 掉帧或花屏时调大（范围 0~60） |
| `qhy.binning` | 1 | 改成 2 会更亮更快，但画面更粗 |

### 跟踪（tracker）

| 参数 | 默认 | 说明 |
|---|---|---|
| `algo` | csrt | 跟踪算法。csrt 最准但最慢，电脑卡的话可以试 kcf 或 mosse |
| `max_lost` | 25 | 连续丢多少帧才判定目标丢失。调大更"执着"，但可能跟错东西 |
| `min_score` | 0.30 | 置信度低于此值算跟丢 |

### 控制（control）

| 参数 | 默认 | 说明 |
|---|---|---|
| `lock_radius_px` | 30 | 画面上绿色准星环的半径（像素） |
| `hold_radius_px` | 30 | **目标进到这个半径内就算追到了，望远镜停止推近**。想跟得更紧就调小，代价是望远镜会更频繁地小幅修正 |
| `deadband_px` | 2 | 偏差小于这个值彻底不动 |
| `pid.kp` | 0.9 | 修正力度。**调大跟得快但容易过冲和抖动，调小更稳但跟得慢**。不懂就别动 |

### 标定（calibration）

| 参数 | 默认 | 说明 |
|---|---|---|
| `step_deg` | 0.30 | 标定时每步转多少度。**视野窄就调小**（目镜相机建议 0.1~0.3），视野宽可以调大。软件会自动往小调，这个值是上限 |
| `settle_s` | 1.2 | 每步之后等望远镜停稳的时间。机械晃动大就调大 |

### 录像（recording）

| 参数 | 默认 | 说明 |
|---|---|---|
| `fps` | 15 | 录像帧率。调高更流畅，文件也更大 |
| `max_minutes` | 60 | 单段最长时长，到点自动停 |

### 网页服务（server）

| 参数 | 默认 | 说明 |
|---|---|---|
| `port` | 8300 | 端口号。**被占用时软件会自动换下一个**，一般不用管 |

---

## 14. 出问题了怎么办

### 窗口打不开，或者只弹出浏览器

软件会依次尝试三种方式：独立窗口 → Edge 应用模式窗口 → 普通浏览器。

**功能完全不受影响**，只是外观不同。想要独立窗口的话：

1. 装一次 Edge WebView2 运行时：<https://developer.microsoft.com/microsoft-edge/webview2/>（选「常青引导程序」）
2. 还是不行的话，把 `data\app.log` 发给开发者——里面记着真实原因

### 连不上望远镜

按顺序查：

1. 手控器**开机**了吗？
2. 线插的是**手控器底部**的小方口，不是底座上的 AUX 口？
3. 串口号选对了吗？（设备管理器 → 端口）
4. 有没有别的软件（比如 Stellarium）占着这个串口？把它关掉
5. USB 转串口的**驱动**装了吗？（CH340 / FTDI）

### 相机打不开

- 被别的软件占用了吗？（QHY 相机同一时刻只能被一个程序打开，把 EZCAP、SharpCap 之类关掉）
- QHY 相机装官方驱动了吗？
- 换个 USB 口试试，别用 USB 集线器

### 跟踪时目标越跑越远

**这是标定方向错了。** 软件会在 3 秒内自动停车并弹提示。

**解决**：对着**静止**的参照物重新标定（点「识别星点」用绿圈那颗）。**标定时千万不要框飞机、云这类会动的东西。**

### 目标进了绿圈望远镜还在转

正常情况下进圈就该停。如果还在小幅抖动，把 `config.yaml` 里的 `hold_radius_px` 调大一点（比如改成 40）。

### 标定总是失败

| 提示 | 解决 |
|---|---|
| 目标离画面边缘太近 | 先用手动微调把目标挪到画面中间 |
| 目标几乎没有移动 | 把 `step_deg` 调大一点 |
| 后目标丢失 | 目标太暗或对比度太低，换一颗亮星 |
| 结果不自洽 / 目标自己移动了太多 | 换成静止目标重标 |

### 更新软件后 UPDATE-Windows.bat 报错

因为你改过 `config.yaml`，而新版也改了同一个文件。在软件目录打开命令行跑：

```bash
git stash push -- config.yaml && git pull --ff-only && git stash pop
```

看到 `Dropped refs/stash` 就是成功了。

### 所有按钮都点不动

多半是登录过期了。刷新页面或重启软件，重新登录。

### 关掉软件后望远镜还在转

新版已经修复这个问题。如果还遇到，**立即关掉望远镜电源**，然后把 `data\app.log` 发给开发者。

---

## 15. 一页速查卡

> 可以打印出来贴在设备旁边。

### 标准操作顺序

```
接线（手控器底部！）→ 开手控器 → 双击 exe → 登录
   ↓
连接望远镜 → 开启视频源
   ↓
识别星点 → 看「星点锐度」调焦（数字越小越好）
   ↓
点绿圈那颗星 → 自动标定（约半分钟，只需做一次）
   ↓
框住要跟的目标 → 开始跟踪
   ↓
（可选）点录制
```

### 关键提醒

| ⚠️ | |
|---|---|
| 串口线插**手控器底部**，不是底座 AUX 口 | |
| 标定必须用**静止**目标，绝不能用飞机和云 | |
| 换过相机/转过相机角度，**必须重新标定** | |
| 绝对不要对着**太阳** | |
| 关软件直接关窗口，「退出登录」只是回登录页 | |

### 出厂账号

`admin` / `admin` —— **第一次登录后立刻改掉**

忘记密码：删掉 `data\users.json` 再启动

### 文件在哪

| 东西 | 位置 |
|---|---|
| 录像 | `data\recordings\` |
| 标定结果 | `data\calibration.json` |
| 运行日志（出问题时发这个） | `data\app.log` |
| 参数配置 | `config.yaml` |
